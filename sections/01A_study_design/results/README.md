# Example results

Outputs of one locked-in run of the scripts in `../scripts/` on the example Final
Report in `../example_data/` and on the demonstration dataset.

Only small files live here. The intermediate conversion products
(`.lgen`, `.ped`, `.bed`) are regenerable and are excluded by `.gitignore`.
