# Example results

Outputs of one locked-in run of the scripts in `../scripts/` on the demonstration
dataset. Small files only; the regional genotypes and the LD matrix are
regenerable and are excluded by `.gitignore`.
