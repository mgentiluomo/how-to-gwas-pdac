# Figures & shared assets

Put manuscript figures here (PNG/PDF, 300 dpi) and the pipeline diagram.
The demo pipeline writes `manhattan.png`, `qqplot.png` and `causal_recovery.tsv`
into this folder by default.
